# What

<!-- One or two sentences: what does this PR change? -->

# Why

<!-- What problem does it solve? Link the issue if there is one (e.g. "Closes #12"). -->

# Checklist

- [ ] `pytest` passes locally
- [ ] `ruff check src tests` is clean
- [ ] Docs updated if behavior changed (README, docstrings, `--help` text)
- [ ] New knowledge-base entries follow the schema in `src/portcullis/kb/__init__.py`
